name: Integration Tests

on:
  schedule:
    - cron: "0 23 * * *" # 7:00 PM EST

jobs:
  run-integration-tests-4.0.0:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: '4.0.0'

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 2.2
          
      - name: Build new SDK
        run: cd auto-generated-sdk && dotnet build --configuration Release
          
      - name: Build Tests
        run: cd tests && dotnet build --configuration Release

      - name: Run Tests
        run: cd tests && dotnet test --no-restore --verbosity normal

      - name: Build Examples
        run: cd examples && dotnet build --configuration Release

  run-integration-tests-v4.2.0:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: 'v4.2.0'

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 2.2
      
      - name: Build new SDK
        run: cd auto-generated-sdk && dotnet build --configuration Release
          
      - name: Build Tests
        run: cd tests && dotnet build --configuration Release

      - name: Run Tests
        run: cd tests && dotnet test --no-restore --verbosity normal

      - name: Build Examples
        run: cd examples && dotnet build --configuration Release

  run-integration-tests-v5.1.0:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
        with:
          ref: 'v5.1.0'

      - name: Setup .NET Core
        uses: actions/setup-dotnet@v1
        with:
          dotnet-version: 2.2
      
      - name: Build new SDK
        run: cd auto-generated-sdk && dotnet build --configuration Release
          
      - name: Build Tests
        run: cd tests && dotnet build --configuration Release

      - name: Run Tests
        run: cd tests && dotnet test --no-restore --verbosity normal

      - name: Build Examples
        run: cd examples && dotnet build --configuration Release
  